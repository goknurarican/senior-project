# V3 - LaBraM with Action-Matched Control

## Purpose
Corrected V2 by using action-matched control epochs (S30/S32 event markers,
3s gap filter). Control and variant epochs now have matched motor demands.

## Key findings
- LOSO AUC: 1.000, ACC: 0.992
- Confound resolved: eye-only drops to 0.502 (from 0.579 in V2)
- Classical band power RF: AUC 0.539 (chance) - signal requires temporal dynamics
- Permutation p<0.0001

## Notes
Primary binary detection result. V5 builds on V3 features for better interpretability.
